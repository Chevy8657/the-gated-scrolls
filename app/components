// the-gated-scrolls/app/components/TimeWealthTracker.tsx
'use client';

import React, { useState } from 'react';

export default function TimeWealthTracker() {
  // Let the broker toggle what their time is actually worth
  const [hourlyWorth, setHourlyWorth] = useState<number>(100);
  
  // Hard benchmark constants from the core engine baseline
  const processingTimeSeconds = 17;
  const adminHoursSavedMonthly = 120;
  
  // Dynamic financial calculation
  const monthlyValueReclaimed = adminHoursSavedMonthly * hourlyWorth;

  return (
    <div style={{
      backgroundColor: '#0D0F14',
      border: '1px solid #1F2833',
      padding: '35px',
      maxWidth: '720px',
      margin: '20px auto',
      boxShadow: '0 10px 30px rgba(0,0,0,0.5)'
    }}>
      {/* COMPONENT HEADER */}
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid #1F2833', paddingBottom: '15px', marginBottom: '25px' }}>
        <h3 style={{ color: '#C5A059', textTransform: 'uppercase', margin: 0, letterSpacing: '1px', fontFamily: 'monospace' }}>
          ⏱️ TIME WEALTH ENGINE
        </h3>
        <span style={{ backgroundColor: '#45A29E', color: '#0B0C10', padding: '4px 8px', fontSize: '0.8rem', fontWeight: 'bold', fontFamily: 'monospace' }}>
          CORE: 17s LOOP
        </span>
      </div>

      {/* THE SCRIPTER INTERACTION */}
      <div style={{ marginBottom: '30px' }}>
        <label style={{ display: 'block', color: '#C5C6C7', marginBottom: '12px', fontSize: '1.1rem', fontWeight: 'bold' }}>
          “Jim, what do you honestly think your time is worth per hour?”
        </label>
        <div style={{ display: 'flex', gap: '15px' }}>
          {[50, 100, 150].map((val) => (
            <button
              key={val}
              onClick={() => setHourlyWorth(val)}
              style={{
                flex: 1,
                backgroundColor: hourlyWorth === val ? '#45A29E' : '#1F2833',
                color: hourlyWorth === val ? '#0B0C10' : '#C5C6C7',
                border: 'none',
                padding: '12px',
                fontWeight: 'bold',
                fontSize: '1.05rem',
                cursor: 'pointer',
                transition: 'all 0.2s ease'
              }}
            >
              ${val} / HR
            </button>
          ))}
        </div>
      </div>

      {/* TELEMETRY READOUTS */}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px' }}>
        
        {/* TIME VELOCITY CARD */}
        <div style={{ background: '#131820', padding: '20px', borderLeft: '4px solid #45A29E' }}>
          <span style={{ color: '#8892B0', fontSize: '0.85rem', display: 'block', textTransform: 'uppercase', letterSpacing: '1px' }}>
            Compliance Cycle Speed
          </span>
          <strong style={{ fontSize: '2.2rem', color: '#C5C6C7', display: 'block', margin: '5px 0' }}>
            17.0 <span style={{ fontSize: '1.1rem', color: '#45A29E' }}>Sec</span>
          </strong>
          <span style={{ color: '#45A29E', fontSize: '0.85rem' }}>
            ↓ Office Friction Destroyed
          </span>
        </div>

        {/* RECLAIMED VALUE CARD */}
        <div style={{ background: '#131820', padding: '20px', borderLeft: '4px solid #C5A059' }}>
          <span style={{ color: '#8892B0', fontSize: '0.85rem', display: 'block', textTransform: 'uppercase', letterSpacing: '1px' }}>
            Monthly Capital Reclaimed
          </span>
          <strong style={{ fontSize: '2.2rem', color: '#C5A059', display: 'block', margin: '5px 0' }}>
            ${monthlyValueReclaimed.toLocaleString()} <span style={{ fontSize: '1.1rem', color: '#C5C6C7' }}>USD</span>
          </strong>
          <span style={{ color: '#C5A059', fontSize: '0.85rem' }}>
            Based on {adminHoursSavedMonthly} Hrs Saved
          </span>
        </div>

      </div>

      {/* CONVERSATIONAL ANCHOR FOOTER */}
      <p style={{ margin: '25px 0 0 0', fontSize: '1.05rem', color: '#C5C6C7', fontStyle: 'italic', textAlign: 'center', lineHeight: '1.5' }}>
        “At ${hourlyWorth}/hr, leaving your floorboards un-automated is costing you exactly <span style={{ color: '#C5A059', fontWeight: 'bold' }}>${monthlyValueReclaimed.toLocaleString()}</span> a month.”
      </p>

    </div>
  );
}
